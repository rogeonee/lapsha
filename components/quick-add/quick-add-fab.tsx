/**
 * iOS / default: no FAB — quick add is a detached native-tab action
 * on iOS 26+. Android renders a Material 3 FAB
 * (quick-add-fab.android.tsx).
 */
export function QuickAddFab(_props: { onPress: () => void }) {
  return null;
}
