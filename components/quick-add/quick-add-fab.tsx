/**
 * iOS / default: no FAB — quick add lives in the tab bar bottom
 * accessory (iOS 26+). Android renders a Material 3 FAB
 * (quick-add-fab.android.tsx).
 */
export function QuickAddFab(_props: { onPress: () => void }) {
  return null;
}
