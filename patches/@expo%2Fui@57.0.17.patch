diff --git a/ios/TextFieldView.swift b/ios/TextFieldView.swift
index ec381d1ac16a287283d1a0e116cc91018c7ba070..422809e6fd1f65feb7a2b5ba73743be1b3f6ca5a 100644
--- a/ios/TextFieldView.swift
+++ b/ios/TextFieldView.swift
@@ -79,7 +79,12 @@ struct TextFieldView: ExpoSwiftUI.View, ExpoSwiftUI.FocusableView {
 
   var body: some View {
 #if !os(tvOS)
-    if #available(iOS 18.0, macOS 15.0, *) {
+    // Lapsha patch: selection-enabled TextField only on iOS 26+. On iOS 18,
+    // SwiftUI's TextField(selection:) fatally asserts (String index is out of
+    // bounds) when the text shrinks under an active selection — fast
+    // backspacing crashes the app despite the clamped binding below.
+    // https://github.com/expo/expo/issues/47434
+    if #available(iOS 26.0, macOS 26.0, *) {
       StatefulSelectableTextField(
         state: props.text,
         selection: props.selection,
