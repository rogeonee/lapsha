import type { ReactElement } from 'react';

/** iOS quick add is a detached native-tab action on iOS 26+. */
export default function QuickAddFab(_props: {
  onPress: () => void;
}): ReactElement | null {
  return null;
}
